{
  "name": "json-toon-parser-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build -w json-toon-parser",
    "test": "vitest --run",
    "lint": "tsc -b packages/json-toon-parser",
    "example": "npm run build && npm run start -w json-toon-example"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "@types/node": "^22.7.4",
    "typescript": "^5.6.3",
    "vitest": "^1.6.0"
  }
}
